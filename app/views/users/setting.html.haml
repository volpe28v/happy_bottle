%div.container-fluid
  %div.row-fluid
    = form_for @user do |f|
      %div
        = f.label :email
        = @user.email

      %div
        = f.label :password
        = f.text_field :password

      = f.submit 'OK', class: "btn"

    = link_to 'Signup', new_user_path
